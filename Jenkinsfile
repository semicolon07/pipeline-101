pipeline {
    agent any

    stages {
        stage ('Compile Stage') {
            echo 'Compile stage'
        }

        stage ('Testing Stage') {
            echo 'Testing stage'
        }


        stage ('Deployment Stage') {
            echo 'Deployment stage'
        }
    }
}